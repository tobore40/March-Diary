

import React, { useState } from 'react';

     const [ navbar, setNavbar] = useState(false);
    const changeBackground = () => {
        if (window.scrollY >= 80) {
            setNavbar(true);
        } else {
            setNavbar(false);
        }
    }

window.addEventListener('scroll', changeBackground);
    return (


         const [navbar, setNavbar] = useState(false);
    const changeBackground = () => {
        if (window.scrollY >= 80) {
            setNavBar(true);
        } else {
            setNavbar(false);
        }
    }

        import React, { useState } from 'react';








        <>
      <div>
        <section className="about section " id="about">
          <h2 className="section-title">About</h2>
          <div className="about__container bd-grid">
            <div className="about__img">
              <motion.img
                src={Logo}
                alt=""
                initial={{ x: "-20vw" }}
                animate={{ x: 0 }}
                transition={{ type: "spring", stiffness: "120" }}
              />
            </div>
            <motion.div
              initial={{ y: -150 }}
              animate={{ y: -10 }}
              transition={{ type: "spring", stiffness: 300 }}
            >
              <h2 className="about__subtitle">
                <span className="soft">Soft</span>Code
              </h2>
              <p className="about__text">
                Hi, I'm Ken a JavaScript Developer, Competitive Programmer, a
                Technical Writer, and an Open Source Enthusiast who is highly
                passionate about building fast, creative applications and
                writing beautiful code. Web Performance and Refactoring are my
                areas of interest and I've been working to enhance my skills.
                Also, I'm a kid who loves to build and design Experiences which
                make peoples life easy
              </p>
            </motion.div>
          </div>
        </section>
      </div>


      #about  {
  background: linear-gradient(rgba(33, 150, 243, 0.5) 0%,rgba(0,0,0,0.7)), url(../about.jpg);
  background-repeat: no-repeat;
  background-size: cover;
  background-attachment: fixed;
  background-position: center;
}
.about__container{
  row-gap: 2rem;
  text-align: center;
}
.about__subtitle{
  margin-bottom: var(--mb-2);
  color: #fff;
}
.about__img{
  justify-self: center;
}
.about__img img{
  width: 200px;
  border-radius: .5rem;
  box-shadow: 3px 3px 7px rgb(240, 214, 214);
}

.soft {
    color: red;
}

.about__text {
  font-size: 20px;
  color: #fff;
}





skills

-webkit-transform: rotate(9deg); /* Safari */
  transform: rotate(9deg);
} 




  
  .about__container, .skills__container{
    grid-template-columns: repeat(2,1fr);
    align-items: center;
    text-align: initial;
  }

 
  .about__img img{
    width: 300px;
  }



  border-top: 0.1875rem solid red !important;